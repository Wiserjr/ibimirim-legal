import{createServer}from'node:http';import{readFile,stat}from'node:fs/promises';import{extname,join,normalize}from'node:path';const root=join(process.cwd(),'public'),mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.webmanifest':'application/manifest+json','.svg':'image/svg+xml'};createServer(async(req,res)=>{try{let p=normalize(decodeURIComponent(req.url.split('?')[0])).replace(/^(\.\.(\/|\\|$))+/,'');if(p==='/'||p==='\\')p='/index.html';const f=join(root,p);const s=await stat(f);const data=await readFile(s.isDirectory()?join(f,'index.html'):f);res.writeHead(200,{'Content-Type':mime[extname(f)]||'application/octet-stream'});res.end(data)}catch{res.writeHead(404);res.end('Não encontrado')}}).listen(8321,'0.0.0.0',()=>console.log('Ibimirim Legal: http://localhost:8321'));
